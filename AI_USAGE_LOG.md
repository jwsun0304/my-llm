# AI 활용 로그

이 프로젝트를 진행하며 Claude Code를 어떻게 활용했는지, 어떤 판단을 직접 내렸는지 기록한다.
형식: 문제 → 왜 AI가 필요했는지 → AI를 어떻게 활용했는지 → 결과를 어떻게 검증/수정했는지.

## 2026-08-08 ~ 09: 프로젝트 초기 세팅

- **문제**: 딥러닝 경험이 없는 상태에서 LLM을 처음부터 구현해야 함. 처음부터 모든 걸 스스로 설계하면 시간이 오래 걸림.
- **왜 AI 필요**: nanoGPT 구조(트랜스포머, self-attention)의 표준 구현 패턴을 빠르게 확보하고, 그 위에서 직접 원리를 이해하는 방식으로 시간을 절약하기 위함.
- **AI 활용**: Claude Code에게 GPT 모델(`model.py`), 학습 루프(`train.py`), 샘플링 스크립트(`sample.py`)의 초안 작성을 요청.
- **검증/판단**:
  - 로컬 환경(Python 3.14, GPU 없음)에서는 PyTorch 실행이 불가능하다는 걸 직접 확인(`nvidia-smi`, `python -c "import torch"` 실행 결과)한 뒤, 학습은 Colab에서 하고 로컬은 코드 작성 전용으로 쓰기로 판단.
  - 체크포인트 저장/재개 로직(`train.py`의 `start_iter` 처리)이 Colab 세션 끊김에 대비해 필요하다고 판단해 반영.
  - `.claude/settings.local.json`처럼 개인 로컬 설정 파일은 GitHub에 올릴 필요가 없다고 판단해 `.gitignore`에 직접 추가.
  - git 커밋 계정 정보(user.name/email)는 AI가 임의로 설정하지 않고 본인이 직접 실행하도록 함 — 계정 정보는 스스로 확인·결정해야 하는 영역이라고 판단.

## 2026-08-09: Colab 첫 학습 실행

- **문제**: Colab 런타임을 처음에 CPU로 잡아서 학습이 비정상적으로 느렸음(3분 넘게 첫 로그도 안 뜸). 원인이 CPU 런타임인지 출력 버퍼링인지 바로 판단이 안 섰음.
- **AI 활용**: Claude Code가 두 가지 가능성(출력 버퍼링, CPU 런타임)을 모두 제시.
- **검증/판단**: 실제로는 CPU 런타임이 원인이었음 — 직접 런타임 설정 화면을 확인해서 원인을 특정한 뒤 GPU(T4)로 전환. AI 제안 중 버퍼링 쪽(`-u` 옵션)은 부가적으로 적용.
- **결과**: GPU 전환 후 정상 학습 진행, 5000 iter까지 완료.

| iter | train loss | val loss |
|---|---|---|
| 0 | 4.3043 | 4.3071 |
| 1000 | 1.4679 | 1.6721 |
| 2000 | 1.2016 | 1.4951 |
| 2250 | 1.1584 | **1.4882 (val 최저점)** |
| 2750 | 1.0745 | 1.4834 |
| 3000 | 1.0334 | 1.4916 |
| 4000 | 0.8545 | 1.5866 |
| 4999 | 0.6526 | 1.7525 |

- **관찰**: iter 2000 부근부터 train loss는 계속 감소(1.20→0.65)하는데 val loss는 iter 2250~2750 부근에서 최저점(≈1.483~1.488)을 찍은 뒤 다시 상승(4999 기준 1.7525)하는 패턴 확인. 명확한 overfitting — train loss만 보면 계속 좋아지는 것처럼 보이지만 실제 일반화 성능은 iter 2500 전후를 정점으로 악화됨. 작은 모델(n_layer=6, n_embd=384)+작은 데이터(tiny-shakespeare, 약 100만 토큰) 조합, 그리고 early stopping/체크포인트 선택 없이 고정 5000 iter를 끝까지 돌린 설정에서 예상 가능한 현상으로 해석. → Ablation/이후 실험에서는 "val loss 최저점 체크포인트를 최종 모델로 선택"하는 방식을 근거로 설명 가능.

## 2026-08-09: 체크포인트 로딩 오류 (torch.load)

- **문제**: `sample.py`로 최종 체크포인트(iter 4999)를 불러올 때 `_pickle.UnpicklingError: Weights only load failed` 발생. `train.py`의 재개 로직에서도 동일 지점에서 터질 수 있는 문제.
- **원인 분석**: PyTorch 2.6부터 `torch.load`의 `weights_only` 기본값이 `False`→`True`로 바뀌어, 체크포인트에 텐서 외에 `GPTConfig` 객체(`train.py`에서 `"config": config`로 통째로 저장)가 들어있으면 기본 보안 모드에서 차단됨.
- **AI 활용**: Claude Code가 에러 메시지의 원인(파이썬/PyTorch 버전 변경 이력)을 짚어주고 수정안(`weights_only=False` 명시) 제시.
- **검증/판단**: 이 체크포인트는 본인이 직접 학습시켜 만든 신뢰 가능한 파일이므로 `weights_only=False`가 안전하다고 판단해 채택 (외부에서 받은 체크포인트였다면 `add_safe_globals` 방식을 썼을 것). `sample.py`, `train.py` 양쪽 모두 수정 후 Colab에서 `git pull`로 반영해 정상 동작 확인.

## Stage 1 마무리 (nanoGPT 학습 결과)

`sample.py` 생성 샘플(온도 0.8, top_k 40)에서 `GLOUCESTER:`, `BRUTUS:`, `CORIOLANUS:` 등 등장인물 이름과 대사 포맷(콜론, 개행 구조)을 정확히 재현했고 셰익스피어체 어휘/운율도 따라갔으나, 문장 단위 의미는 대부분 맞지 않음 — char-level, 소형 모델(6-layer, 384-dim)이 문법적 패턴은 학습했지만 의미적 일관성까지는 확보하지 못한 상태로 해석. 위 overfitting 관찰과 함께 "표면적 패턴 vs 의미적 이해"를 구분해 설명할 수 있는 근거로 확보.

## 2026-08-10: Stage 2 Ablation 실험 (positional encoding / causal mask 제거)

- **문제**: 모델이 "그냥 돌아가는 것"과 "왜 각 구성요소가 필요한지"는 다른 문제. positional encoding과 causal mask가 실제로 성능에 어떤 영향을 주는지 정량적으로 증명하고 싶었음.
- **왜 AI 필요**: `GPTConfig`에 `use_pos_emb`/`use_causal_mask` 플래그를 추가해 모델 구조를 건드리지 않고 두 요소를 켜고 끌 수 있게 만드는 리팩터링, 그리고 `train.py`의 학습 루프를 `train_model()` 함수로 재사용 가능하게 분리하는 작업을 Claude Code에 요청.
- **AI 활용**: 3개 variant(baseline / no_pos_emb / no_causal_mask)를 동일 조건(1500 iter, 동일 batch/lr)으로 학습하는 `ablation.py` 작성.
- **검증/판단**: Colab 세션이 두 번 끊기면서 처음부터 다시 돌려야 하는 문제를 겪음 → 체크포인트/결과 CSV 저장 경로를 환경변수(`CKPT_DIR`, `RESULTS_DIR`)로 오버라이드 가능하게 만들어 Google Drive에 저장하도록 수정, 이미 끝난 variant는 재실행 시 건너뛰도록 `ablation.py`에 스킵 로직 추가 — 세션 안정성 문제를 직접 겪고 나서 판단해 반영.
- **결과** (각 variant 1500 iter 학습, val loss 기준):

| variant | 최종 train loss | 최종 val loss |
|---|---|---|
| baseline | 1.3403 | 1.5605 |
| no_pos_emb | 1.4063 | 1.6394 |
| no_causal_mask | 0.0101 | **0.0101 (iter 450 부근에서 급락)** |

- **해석**:
  - `no_pos_emb`는 학습 내내 baseline보다 꾸준히 나쁨 (val loss +0.08) — 위치 정보 없이는 토큰 순서를 활용하지 못해 성능이 확실히 저하됨을 확인.
  - `no_causal_mask`는 iter 300~450 사이에서 val loss가 2.4 → 0.016으로 급락 후 0.01 부근에 고정. 이는 성능이 좋아진 게 아니라, causal mask가 없으면 attention이 예측 대상인 다음 위치의 토큰을 직접 참조할 수 있어 모델이 "베끼기" shortcut을 학습한 것 — 즉 정답 유출(label leakage). loss는 거의 0이지만 실제 생성 시점(autoregressive)에는 미래 토큰이 존재하지 않으므로 이 모델은 텍스트 생성 능력이 전혀 없음. → causal mask가 성능 향상용이 아니라 **autoregressive 언어모델이 성립하기 위한 구조적 필수조건**이라는 것을 수치로 증명.

## 2026-08-10: Stage 3 모델 크기-비용 트레이드오프 (Optuna) + GPU 메모리 병목 분석

- **문제**: Optuna를 "val loss를 최소화하는 최적 하이퍼파라미터 찾기"로만 쓰면 흔한 튜토리얼 사용법에 그침. 반도체/하드웨어 배경과 연결지어 "모델을 키우면 성능이 얼마나 좋아지고, 그 대가로 학습 비용(시간/메모리)은 얼마나 늘어나는가"를 정량적으로 보고 싶었음.
- **왜 AI 필요**: Optuna 탐색 루프 안에서 val loss뿐 아니라 파라미터 수, 학습 wall-clock 시간, GPU peak 메모리(`torch.cuda.max_memory_allocated()`)까지 같이 기록하는 `search.py` 작성, 그리고 실제 T4에서 OOM이 나는 지점을 batch size를 2배씩 늘려가며 찾는 `memory_probe.py` 작성을 Claude Code에 요청.
- **AI 활용**: `search.py`는 n_layer(2~8)/n_head(2~6)/head_size(32~64) 조합 6개를 800 iter씩 학습해 비교. `memory_probe.py`는 8-layer/512-dim(25.4M 파라미터) 모델 고정, batch 64→4096으로 늘리며 forward+backward 1스텝씩 실행.
- **결과 — 모델 크기 vs 비용/성능** (`results/search_results.csv`, val loss 기준 정렬):

| n_layer | n_head | n_embd | 파라미터 수 | 학습시간(초) | peak mem(MB) | val loss |
|---|---|---|---|---|---|---|
| 4 | 4 | 256 | 3.26M | 148.8 | 1898.1 | **1.8113 (최고)** |
| 6 | 4 | 192 | 2.74M | 165.9 | 2311.8 | 1.8941 |
| 8 | 6 | 192 | 3.63M | 251.2 | 3649.0 | 1.9126 |
| 4 | 2 | 128 | 0.84M | 59.4 | 958.3 | 2.0843 |
| 2 | 4 | 128 | 0.45M | 42.3 | 675.5 | 2.1933 |
| 4 | 2 | 96 | 0.48M | 46.0 | 794.1 | 2.2104 |

- **결과 — GPU 메모리 병목** (`results/memory_probe.csv`, 8-layer/512-dim 고정):

| batch size | 상태 | peak mem |
|---|---|---|
| 64 | ok | 7202.8 MB |
| 128 | ok | 14255.8 MB |
| 256 | **OOM** | - |

- **해석**:
  - 가장 좋은 val loss(1.81)를 낸 건 파라미터가 가장 많은 모델(trial 4, 8-layer/3.63M)이 아니라 **4-layer/256-dim(3.26M)** 모델. 오히려 레이어를 8개까지 늘린 모델은 비슷한 파라미터 수의 4-layer 모델보다 성능은 더 나쁘면서 학습 시간은 1.7배(251초 vs 149초) 더 걸림 — 이 데이터셋(tiny-shakespeare, ~100만 토큰) 규모에서는 "깊게 쌓기"보다 "적당히 넓게"가 더 효율적이라는 근거. 레이어가 순차적으로 계산돼야 해서 깊이가 늘어날수록 같은 파라미터 예산 대비 GPU 병렬성을 덜 활용하게 된다는 하드웨어적 해석도 가능.
  - batch 64→128로 2배 늘리자 peak 메모리도 정확히 약 2배(7.2GB→14.3GB)로 증가 — attention 연산량이 batch size에 선형적으로 비례한다는 예상과 일치. batch 128(14.3GB)이 이미 T4 가용 메모리(~15GB) 턱밑까지 차서, batch 256에서 바로 OOM 발생. → "왜 batch size를 64로 정했는가"라는 질문에 실측 데이터로 답변 가능 (더 큰 모델을 쓸 경우 batch size를 반드시 줄여야 하는 실질적 한계선을 직접 확인).
- **검증/판단**: Optuna의 TPE 샘플러 기본 설정(trial 6개)은 탐색 초반이라 사실상 랜덤에 가까운 샘플링이었음 — "최적값을 찾았다"보다 "여러 크기에서 성능-비용 관계의 경향을 관찰했다"는 것으로 결과를 해석하는 게 정확하다고 판단. Optuna 결과의 `best_trial`을 그대로 "최적 모델"이라 부르지 않고 트레이드오프 표 전체를 근거로 삼음.

## 2026-08-10: Stage 2.5 BPE 토크나이저 구현 + char-level 비교

- **문제**: Stage 1~3까지는 char-level 토크나이저만 써봤는데, "왜 char-level을 골랐는가"에 실질적 답이 "간단해서"뿐이었음. 실제 LLM들이 왜 subword(BPE) 토크나이저를 쓰는지 직접 실험으로 이해하고 싶었음. (v4→v5 계획 수정 때 "LLM 전문가가 파고들 때 방어력이 약한 지점"으로 스스로 지적한 부분.)
- **왜 AI 필요**: minBPE 스타일 byte-pair encoding(merge 학습 → encode/decode)을 처음부터 구현하는 코드(`src/bpe.py`)와, 서로 다른 vocab 크기의 두 토크나이저를 동일 모델 설정으로 학습해 공정 비교하는 스크립트(`src/compare_tokenizer.py`) 작성을 Claude Code에 요청.
- **AI 활용**: `bpe.py`(BPE 학습/인코딩/디코딩, encode-decode 왕복 정확성 assert 포함), `prepare_data_bpe.py`(vocab_size=512로 학습 데이터 생성), `compare_tokenizer.py`(char vs bpe를 동일 4-layer/256-dim 설정으로 1500 iter씩 학습).
- **결과** (`results/tokenizer_compare.csv`):

| tokenizer | vocab_size | chars/token | 문맥(256 토큰 기준 실제 글자 수) | val loss (원시) | **bits-per-char (공정 비교)** |
|---|---|---|---|---|---|
| char | 65 | 1.000 | 256자 | 1.5852 | 2.287 |
| bpe | 512 | 1.963 | 502.5자 | 2.9952 | **2.2013** |

- **해석**:
  - 표면적 val loss만 보면 BPE(2.995)가 char-level(1.585)보다 훨씬 나빠 보이지만, 이는 vocab 크기가 다른 두 모델의 cross-entropy를 그대로 비교하는 것이 잘못된 방법이기 때문 — vocab이 클수록 무작위 초기 loss 자체가 높음(ln(512)≈6.29 vs ln(65)≈4.17, 실제 iter 0 값 6.2882/4.1841과 일치). 이를 바로잡기 위해 **bits-per-character**(= loss_nats / chars_per_token / ln(2))로 정규화해서 비교해야 함을 직접 판단하고 적용.
  - 정규화 후에는 BPE(2.201 bpc)가 char-level(2.287 bpc)보다 **더 낮음(더 좋음)** — 심지어 BPE 모델이 아직 수렴 중(iter 1499에도 loss가 눈에 띄게 계속 감소 중)인데도 이미 앞섬. 게다가 `block_size=256`(토큰 수 고정)이 실제로 커버하는 문맥 길이가 char-level은 256자, BPE는 502.5자로 거의 2배 — 동일한 attention 연산 비용(둘 다 T=256)으로 2배 넓은 문맥을 확보. → 실제 LLM들이 char-level이 아니라 subword 토크나이저를 쓰는 이유를 숫자로 재현.
- **검증/판단**: encode한 토큰을 다시 decode했을 때 원문과 100% 일치하는지(`roundtrip check`)를 `bpe.py`에 직접 assert로 넣어, 토크나이저 구현 자체의 정확성을 학습 실험 전에 먼저 검증함.

## 2026-08-10: Stage 3.5 스케일링 법칙 미니 실험

- **문제**: Stage 3(Optuna)는 파라미터 수 축만 봤는데, 실제 LLM 연구의 핵심 개념인 "스케일링 법칙"(모델 크기·데이터 크기 두 축이 성능에 거듭제곱 법칙으로 영향을 준다는 것, Chinchilla/Kaplan 등)과 연결이 안 되어 있었음. v4→v5 계획 수정 때 스스로 지적한 약점.
- **왜 AI 필요**: `search.py` 인프라를 재사용해 모델 크기 3단계 × 학습 데이터 비율 3단계(25/50/100%) 조합 9개를 학습하고, 로그-로그 스케일 기울기까지 계산하는 `scaling_law.py` 작성을 Claude Code에 요청. `train_model()`에 이미 있던 `train_data` 파라미터로 데이터 전체를 다시 준비할 필요 없이 배열을 슬라이싱만 해서 재사용.
- **결과** (`results/scaling_law.csv`, 각 조합 600 iter):

| 모델 크기 (파라미터) | data 25% | data 50% | data 100% |
|---|---|---|---|
| small (0.45M) | 2.4028 | 2.3278 | 2.2971 |
| medium (3.26M) | 2.1369 | 2.0568 | **1.9530 (전체 최저)** |
| large (10.8M) | 2.1861 | 2.0552 | 1.9884 |

- **해석**:
  - **데이터 축**: 세 모델 크기 모두에서 데이터를 늘릴수록 val loss가 깨끗하게, 단조적으로 감소 (예: large 2.19→2.06→1.99). 로그-로그 기울기 `loss ~ train_tokens^(-0.068)` — 방향은 스케일링 법칙 문헌과 일치.
  - **파라미터 축**: 그러나 전체 데이터 기준으로 파라미터를 늘렸을 때는 단조 감소가 깨짐 — **medium(3.26M)이 large(10.8M)보다 오히려 val loss가 더 낮음**(1.9530 vs 1.9884). 로그-로그 기울기도 `-0.049`로 매우 얕고 사실상 신뢰하기 어려움.
  - 이 비단조성은 "큰 모델이 원래 안 좋다"는 뜻이 아니라, **모든 크기를 동일한 600 iter로 고정**해서 학습시켰기 때문 — 파라미터가 많을수록 그 용량을 다 쓰려면 더 많은 학습 스텝/데이터가 필요한데, 여기서는 큰 모델일수록 상대적으로 덜 학습된(undertrained) 상태에서 실험이 끝난 것. 이는 Chinchilla 논문이 제시한 "고정된 연산 예산에서는 모델 크기와 학습 데이터/스텝을 비례해서 늘려야 한다(compute-optimal)"는 개념을 그대로 재현한 결과로 해석 — 억지로 3점짜리 매끈한 법칙을 만들어내기보다, **왜 안 맞았는지**를 설명하는 쪽이 더 정확하고 방어 가능한 결론이라고 판단.
- **검증/판단**: 3개 데이터 포인트로만 fitting한 로그-로그 기울기는 통계적으로 신뢰구간이 없는 "추세 관찰" 수준이라는 것을 명확히 인지 — `scaling_law.py`의 출력과 이 로그 모두에서 "N=3점" 한계를 명시해 과장 해석을 피함.

## 2026-08-10: Stage 4 RTL RAG 어시스턴트 + hallucination 검증

- **문제**: 본인이 실제로 보유한 두 RTL 프로젝트(X25519 ECDH 가속기, LBIST 재현 프로젝트)를 지식베이스로 하는 RAG 질의응답 시스템을 만들고, "그럴듯해 보이는지"가 아니라 "실제로 근거 있는 답을 하는지"를 정량적으로 검증하고 싶었음.
- **왜 AI 필요**: 청킹(`rag_index.py`, .v/.c/.py/.md 파일을 40줄 단위·8줄 overlap으로 분할) → 임베딩(all-MiniLM-L6-v2) → 코사인 유사도 검색 → 소형 LLM(Qwen2.5-1.5B-Instruct) 생성 파이프라인(`rag_ask.py`)과, 12문항 테스트셋으로 자동 채점하는 `rag_eval.py` 작성을 Claude Code에 요청.
- **AI 활용/검증 과정**:
  - 테스트셋(`data/rag_test_set.json`)은 ECDH는 제가 직접 소스 코드를 읽고, LBIST는 README + 핵심 코드(`prpg_misr.py`, `adaptive_controller.py`, `scan_groups.py`)를 실제로 읽어 사실 관계를 대조한 뒤 작성 — README만 보고 만들지 않음.
  - 1차 실행(top_k=3): retrieval hit 75%(9/12), keyword 기반 grounding hit 50%(6/12).
  - 검색 실패 사례(2, 3, 11번)를 보고 top_k를 3→5로 늘려 2차 실행 → 자동 지표는 grounding 50%→75%로 개선됐으나, **12개 답변을 전부 직접 다시 읽고 수동으로 재채점한 결과 실제 정답률은 거의 그대로(약 5/12, ~42%)** 였음. 오히려 문맥을 더 준 것이 아래 같은 새로운 오류를 만들어냄:
    - 9번: "결함 수 2,911개→67.00개" — 67.00은 실제로 다른 회로(s9234)의 **커버리지 %** 값인데 결함 개수로 착각해 섞음.
    - 10번: 숫자(95.595%, 94.581%, 6.95%p) 자체는 모두 실제 문서에 있는 값인데, **서로 다른 두 실험(단일시드 비교 vs 튜닝-전후 비교)의 결과를 조합해 "Adaptive가 항상 이겼다 + 6.95% 더 높다"는 자기모순적 결론**을 만듦 (나열한 숫자상 Fixed-T가 더 높은데도).
    - 5번: RFC 7748·RFC 8037·Wycheproof 세 출처 모두 "518개"라고 답변(top_k=3일 때는 두 개만 섞었는데 top_k=5에서 오히려 세 개 다 섞임).
  - 키워드 매칭 기반 grounding 체크(`rag_eval.py`)의 한계를 실제로 발견: 답변에 정답 관련 단어가 "포함되어 있는지"만 보기 때문에, **숫자들의 관계·귀속이 틀린 경우(위 9, 10, 5번)를 전혀 못 잡아냄** — 자동 지표가 실제보다 낙관적으로 나온 근본 원인.
  - 반대로 자동 지표가 과소평가한 경우도 확인: 3번(PQC_KEM.c)과 11번(PRPG 비트 수 관련 초기 시도)은 검색이 실패했음에도 모델이 지어내지 않고 "근거를 찾을 수 없습니다"라고 정직하게 거부함 — 이는 시스템 프롬프트가 의도대로 작동한 **바람직한 실패**인데, retrieval_hit=False로 기계적으로 "실패"와 동일하게 집계됨.
- **최종 판단**: top_k를 더 키우는 방향으로 반복 튜닝하지 않기로 결정. 근거 — top_k를 늘려도 진짜 정답률은 개선되지 않았고, 오히려 1.5B급 소형 LLM이 여러 사실을 동시에 다룰 때 **관계/귀속을 혼동하는 것 자체가 병목**임을 확인했기 때문(retrieval 문제가 아니라 generator의 다중 사실 결합 능력 문제). 이는 Stage 4.5(RAG vs LoRA 비교)로 이어지는 자연스러운 근거가 됨 — 검색 증강만으로는 한계가 있고, 프로젝트 특화 사실을 모델 파라미터에 직접 학습(파인튜닝)시키는 접근과 비교해볼 필요가 있다는 결론.
- **결과 요약** (`results/rag_eval.csv`, top_k=5 최종 기준, 수동 재채점 포함):

| 구분 | 문항 | 개수 |
|---|---|---|
| 완전히 정확 | 1, 4, 8, 11 | 4 |
| 근거 없음 → 정직한 거부 (바람직) | 3 | 1 |
| 부분 오류 (핵심은 맞지만 일부 수치/개수 틀림) | 6, 7 | 2 |
| 완전 환각 (자동지표는 놓침) | 5, 9, 10 | 3 |
| 완전 환각 (자동지표도 잡음) | 2 | 1 |
| 과도한 거부 (근거가 있는데도 거부) | 12 | 1 |

- **테스트셋 자체의 한계도 발견**: 11번 문항의 `expected_files`에 `lbist/prpg_misr.py`(Python 시뮬레이션)만 넣고 실제로 존재하는 `lbist/prpg.v`(Verilog 구현)를 빠뜨려서, 정답을 정확히 맞혔는데도 retrieval_hit=False로 잘못 채점됨 — 테스트셋도 한 번에 완벽할 수 없고 만들면서 계속 보정해야 한다는 것을 실제로 겪음.

## 2026-08-10: 계획 재검토 — Stage 4.5 우선순위 하향, 문서화로 전환

- **문제**: Stage 1~4를 다 끝낸 시점에서, 계획대로 Stage 4.5(RAG vs LoRA 비교)를 계속 진행하는 게 맞는지, 아니면 지금까지 쌓은 결과를 문서화하는 게 더 나은지 판단이 필요했음. 또한 "AI를 쓰는 이유는 시간/비용 절감인데 이 프로젝트에 그 근거를 접목할 수 있는가"라는 질문도 있었음.
- **AI 활용**: Claude Code에게 두 선택지(Stage 4.5 진행 vs 문서화 우선)를 차별화 기여도/리스크/완료 가능성 세 기준으로 점수화해서 비교해달라고 요청.
- **판단 근거**: Stage 4.5가 처음 계획될 때 기대했던 역할("RAG의 한계를 보여주는 근거")을 Stage 4의 hallucination 수동 재채점 결과가 이미 충분히, 오히려 더 정교하게 해냄(자동 지표의 한계까지 발견). 반면 Stage 4.5는 애초에 "가장 크고 리스크 있는 추가"로 표시해뒀던 단계. 점수화 결과 Stage 4.5 실제 실행(4/10) < 이론만 서술(6/10) < 문서화+AI활용 시간단축 근거 추가(9/10)로 나와, 문서화를 최우선으로 전환하기로 결정. Stage 4.5는 폐기하지 않고 "여유 있으면 나중에" 수준으로 다운그레이드.
- **AI 활용 시간단축 근거**: git 커밋 타임스탬프(조작 불가능한 객관적 증거)를 직접 확인해, 2026-08-10 14:10~17:08(2시간58분) 사이에 4개의 독립된 실험(Optuna+메모리병목, BPE, 스케일링법칙, RAG+환각검증)을 설계부터 문서화까지 끝냈다는 사실을 README에 명시하기로 판단. 동시에 같은 날 오전(09:26~12:56, 약 3시간30분)은 Colab 세션/Drive 마운트 트러블슈팅에 쓰였다는 것도 대조군으로 같이 기록 — "AI가 모든 걸 빠르게 해준다"는 과장이 아니라 "코드 초안 작성 시간은 줄지만 인프라 문제는 그대로 겪는다"는 균형 잡힌 결론을 내기 위함.
- **검증**: 시간 수치는 추정이 아니라 `git log --date=format`으로 직접 뽑은 실제 커밋 시각을 그대로 사용 — 면접에서 "그 시간 단축이 진짜냐"는 질문에 GitHub 커밋 히스토리를 그대로 보여주는 것으로 답변 가능.

## 2026-08-10: Stage 4.5 RAG vs LoRA 파인튜닝 비교 (결국 진행함)

- **문제**: 우선순위를 낮췄던 Stage 4.5를 실제로 진행하기로 함 — Stage 4에서 확인한 RAG의 한계(관계/귀속 오류)가 파인튜닝으로 해결되는지, 아니면 파인튜닝이 오히려 다른 위험을 만드는지 직접 비교해보고 싶었음.
- **설계상 핵심 판단**: LoRA 학습 데이터(`data/lora_train_set.json`, 28개 QA)를 평가용 12문항(`rag_test_set.json`)과 **의도적으로 겹치지 않게** 구성함 — 같은 사실을 학습시키고 맞혔다고 하면 순환 검증이 되기 때문. 같은 도메인(ECDH/LBIST)의 다른 사실들로 채워서 "관련 있지만 못 본 질문에 일반화하는지"를 보는 실험으로 설계.
- **AI 활용**: `lora_finetune.py`(HuggingFace PEFT로 Qwen2.5-1.5B-Instruct에 LoRA 적용, r=8, q/k/v/o_proj, 6 epoch), `rag_vs_lora_eval.py`(같은 12문항을 LoRA만 / RAG+LoRA 두 조건으로 실행) 작성.
- **트러블슈팅**: `get_peft_model()` 호출 시 `peft`가 Colab에 미리 깔린 `torchao` 버전(0.10.0)을 보고 `ImportError`를 던짐(단순 "미지원"이 아니라 하드 에러). `requirements.txt`에 `torchao>=0.16.0` 명시로 해결.
- **결과** (`results/rag_vs_lora_eval.csv`, 자동 키워드 지표 + 수동 재채점 병기):

| 조건 | 자동 지표 | 수동 재채점 정답률 |
|---|---|---|
| RAG only (Stage 4, top_k=5) | 75% | ~42% (5/12) |
| **LoRA only (검색 없음)** | 25% | **~17% (2/12)** |
| **RAG + LoRA** | 41.7% | **~42% (5/12, RAG only와 동일한 4문항 정답)** |

- **해석**:
  - **LoRA만 썼을 때가 가장 위험함.** 자동 지표(25%)보다 실제로 더 나쁨(17%) — 특히 두 사례가 심각함: curve25519.v의 작성자를 묻자 실존하지 않는 **"김동현"이라는 사람 이름을 지어냄**, LBIST의 FPGA 검증 여부를 묻자 **"2018년 3월~2019년 4월, FPGA 보드 5개 사용"이라는 구체적이지만 완전히 허위인 타임라인을 창작**함. 28개 예시로 파인튜닝하면 모델이 "이 도메인에 대해 자신 있게 단정적으로 답하는 말투"는 배우지만, 정작 학습하지 않은 새 사실에는 그 자신감만 전이되고 근거는 전이되지 않아 오히려 더 위험한 환각을 만든다는 것을 확인.
  - **RAG+LoRA는 RAG only 대비 개선이 없음.** 수동 재채점 기준 정확히 같은 4문항(1, 4, 8, 11)을 맞혔고 나머지는 동일하게 틀림 — 파인튜닝이 검색 증강 위에 추가 가치를 주지 못함.
  - **결론**: 이 정도 규모(수십 개)의 사실 기반 QA 데이터로는, 검색 없이 파인튜닝만으로 사실을 안정적으로 내재화시키는 것은 위험하고, 검색과 결합해도 이득이 없다. **좁고 정적인 지식베이스에 대한 사실 질의응답에는 파인튜닝보다 RAG가 우선**이라는, "RAG와 파인튜닝을 언제 쓰는가"라는 실무 질문에 대한 실증적 답을 얻음. (참고로 파인튜닝이 유리한 영역은 "말투/형식/특정 태스크 수행 방식"처럼 정확한 사실 회상이 아닌 경우인데, 이번 실험은 그 반대 케이스인 순수 사실 회상 QA였다는 점도 이 결론의 조건으로 명시해야 함.)
- **검증/판단**: 이번에도 자동 키워드 지표만 믿지 않고 12x2=24개 답변을 전부 수동 재검토함. 8번 문항은 내용이 맞는데 키워드 불일치로 자동지표가 False로 잘못 판정(false negative), 10번 문항은 내용이 틀렸는데 "Fixed-T"라는 단어가 들어있어 True로 잘못 판정(false positive) — Stage 4에서 발견한 "키워드 매칭 지표의 한계"가 이번에도 동일하게 재현됨을 재확인.

## (진행하며 계속 추가)

- (현재 계획된 추가 실험 없음 — Stage 1~4.5 및 문서화 완료)
